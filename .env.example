# StreamScanner Pusher credentials
PUSHER_KEY=1abcdc382aa1ff65c7be
PUSHER_CLUSTER=us3
PUSHER_CHANNEL=sschat_c2a6e2feefc3c81a79a80b557bddb84f

# Server ports
TCP_PORT=10000
API_PORT=9000

# IP whitelist (comma-separated)
ALLOWED_IPS=127.0.0.1

# Grafana Loki (only sends logs in production)
LOKI_URL=https://logs-prod-021.grafana.net
LOKI_USER=
LOKI_TOKEN=
LOKI_JOB=c64-terminal-server

# Environment
NODE_ENV=development

# App version (set by Docker build, 4-char git hash)
APP_VERSION=0000
